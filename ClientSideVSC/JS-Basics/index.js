let name = "Tobias";

console.log(name);

// Cannot be reserved Keyword
// Should be meaningful - variable name should be explanatory of what it represents 
// Cannot start with a number
// Cannot contain a space or hyphen (-)
// Case sensitive

// Use camel case:
let firstName = "tobi"; 
lastName = "besser";

const interestRate = 0.3;
interestRate = 1;
console.log(interestRate);

// Best practice, if value doesnt have to be reassigned use 'const' else use 'let'